<!DOCTYPE html>
<html>
    <body>
        <h2>Album: {{album.name}}</h2>
            <ul>
                {% for img in album.photo_set.all %}
                <li><a href="{% url 'imageDetail' img.id %}"><img src="{{img.imgPreview.url}}" alt="{{img.name}}" height="{{img.imgPreview.height}}" width="{{img.imgPreview.width}}"/></a></li>
                {% endfor %}
            </ul>
    </body>
</html>